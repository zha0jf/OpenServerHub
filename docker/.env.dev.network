# 开发环境配置 - 网络访问配置
# 复制此文件为 .env.dev 并修改以下配置

# 服务器IP地址配置
# 设置为您的Linux服务器IP地址，这样您可以从Windows PC访问服务
# 例如：SERVER_IP=192.168.1.100
SERVER_IP=10.10.0.1

# 前端API配置
# 前端将使用此地址连接后端API
REACT_APP_API_URL=http://${SERVER_IP}:8000
REACT_APP_WS_URL=ws://${SERVER_IP}:8000

# Grafana URL配置
REACT_APP_GRAFANA_URL=http://${SERVER_IP}:3001

# CORS配置
# 允许来自开发环境的跨域请求
# 注意：在生产环境中应使用具体的域名而不是通配符
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://${SERVER_IP}:3000,http://${SERVER_IP}:3001

# 数据库配置（开发环境使用SQLite）
DATABASE_URL=sqlite:///./data/openserverhub.db

# 应用配置
SECRET_KEY=your-secret-key-here-change-this-in-development
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=DEBUG

# IPMI配置
IPMI_CONNECTION_POOL_SIZE=500
IPMI_TIMEOUT=10
IPMI_RETRY_COUNT=3

# 定时任务配置
SCHEDULER_ENABLED=true
POWER_STATE_REFRESH_INTERVAL=3
POWER_STATE_REFRESH_ENABLED=true

# 监控配置
MONITORING_ENABLED=true
MONITORING_INTERVAL=10
PROMETHEUS_URL=http://prometheus:9090
GRAFANA_URL=http://grafana:3000
GRAFANA_API_KEY=your-grafana-api-key-here
PROMETHEUS_TARGETS_PATH=/etc/prometheus/targets/ipmi-targets.json