# Configuration file for ipmi_exporter

modules:
  default:
    collectors:
    - bmc
    - ipmi
    - dcmi
    - chassis
    exclude_sensor_ids:
    - 2    # 排除特定传感器
  dell_servers:
    collectors:
    - bmc
    - ipmi
    - dcmi
    - chassis
    - sel  # 系统事件日志